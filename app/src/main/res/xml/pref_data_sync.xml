<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
            android:key="sync_frequency"
            android:title="@string/pref_title_sync_frequency"
            android:entries="@array/pref_ad_frequency_titles"
            android:entryValues="@array/pref_ad_frequency_values"
            android:defaultValue="0"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"/>

    <ListPreference
            android:key="sync_power"
            android:title="@string/pref_title_sync_power"
            android:entries="@array/pref_ad_power_titles"
            android:entryValues="@array/pref_ad_power_values"
            android:defaultValue="0"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"/>

</PreferenceScreen>
